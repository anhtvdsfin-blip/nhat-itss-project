# Backend (placeholder) for JT-Helper

Run:
1. cd d:\Project\NHAT-ITSS\backend
2. npm install
3. npm run dev   # starts with nodemon on port 4000
   or
   npm start     # production

API:
- GET  /api/ping
- POST /api/analyze    { text }
- POST /api/translate  { text }
- POST /api/vocab      { word }

Note: Responses are placeholders. Replace logic with real NLP/translation calls as needed.
